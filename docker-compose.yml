version: '3.8'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  ml_client:
    build:
      context: .
      dockerfile: Dockerfile.ml_client
    ports:
      - "5002:5002"
    environment:
      - MONGO_URI=mongodb://mongodb:27017
      - MONGO_DBNAME=MLAPP
      - MODEL=ssd_mobilenet_v2_fpnlite_320x320_coco17_tpu-8/saved_model
    depends_on:
      - mongodb

  web-app:
    build:
      context: .
      dockerfile: Dockerfile.web-app
    ports:
      - "5001:5001"
    environment:
      - MONGO_URI=mongodb://mongodb:27017
      - MONGO_DBNAME=MLAPP
    depends_on:
      - mongodb
